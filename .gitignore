# Compiled binaries
/nctalk
/nctalk-call
/nctalk-talk
/spike-check
*.cstemp
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary / coverage
*.test
*.out
coverage.txt

# Go workspace
go.work
go.work.sum

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Local env / secrets — никогда не коммитить
.env
.env.*

# WebRTC-call аудио/медиа-артефакты (тестовые записи, спектрограммы, TTS)
*.pcm
*.wav
*.aiff
*.m4a
*.ogg
*.mp3
*.png

# Spike/debug-артефакты: логи прогонов, браузер-наблюдатели playwright-MCP,
# отвергнутый playwright-через-npm (заменён на playwright MCP extension).
*.log
pw-webrtc.mjs
.playwright-mcp/

# Local session-state (управляется через /checkpoint, не версионится в git)
docs/session-state/

